import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#D8BAA9",
    },
    secondary: {
      light: "#A7A9AB",
      main: "#E7E5D9",
    },
  },
});

export default theme;
